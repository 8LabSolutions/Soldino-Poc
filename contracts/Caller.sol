pragma solidity ^0.5.0;

import "./Proxy.sol";

contract Caller {
    Proxy proxy;

    /*function Caller(address _proxyAddress) public {
        proxy = Proxy(_proxyAddress);
    }
    function go(address _usAddress) public {
        //proxy.userIsRegistered(_usAddress);
    }
*/}